def Add(a, b):
    """Add
    Parameters:
      a = number(int)
      b = number(int)
    Returns:
      sum of a and b
    """
    return a+b
print Add(1,2)
a=Add(2,3)
b=''''''
print str((Add.__doc__))
print repr((Add.func_doc))
import pdb; pdb.set_trace()
